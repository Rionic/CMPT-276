// returns the larger of the two arguments
int max( int a, int b) {
  if( a > b )	return a;
  return b;
}

// returns the smaller of the two arguments
int min( int a, int b) {
  if( a < b ) 	return a;
  return b;
}
